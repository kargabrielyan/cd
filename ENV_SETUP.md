# 📝 Настройка .env файла

## Где находится .env файл?

`.env` файл должен находиться в **корне проекта** (там же где `package.json`):

```
C:\AI\CentralDispatch\.env
```

## ⚠️ Важно

`.env` файл **не коммитится** в Git (он в `.gitignore`), поэтому его нужно создать вручную.

---

## 🔧 Создание .env файла

### Вариант 1: Создать вручную

1. Создай файл `.env` в корне проекта: `C:\AI\CentralDispatch\.env`
2. Скопируй содержимое из `.env.example` (если есть)
3. Заполни реальными значениями

### Вариант 2: Через командную строку

```bash
# В PowerShell (в корне проекта)
cd C:\AI\CentralDispatch
copy .env.example .env
# Затем отредактируй .env файл
```

---

## 📋 Переменные окружения для проекта

Судя по коду, нужны следующие переменные:

### Обязательные:

```env
# Режим работы
NODE_ENV=development

# Порт сервера
PORT=3000

# Настройки Email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_TO=gabrielyankaro67@gmail.com
```

### Опциональные:

```env
# URL API (если нужно)
NEXT_PUBLIC_API_URL=http://localhost:3000
```

---

## 📝 Пример .env файла

Создай файл `.env` в корне проекта со следующим содержимым:

```env
# Режим работы (development или production)
NODE_ENV=development

# Порт для Next.js сервера
PORT=3000

# Настройки Email для отправки кодов подтверждения
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-gmail-app-password
EMAIL_TO=gabrielyankaro67@gmail.com

# URL API
NEXT_PUBLIC_API_URL=http://localhost:3000
```

---

## 🔐 Настройка Gmail для отправки Email

Если используешь Gmail, нужно:

1. Включить двухфакторную аутентификацию
2. Создать "Пароль приложения" (App Password):
   - Перейти: https://myaccount.google.com/apppasswords
   - Создать пароль для "Почта"
   - Использовать этот пароль в `EMAIL_PASS`

**Пример:**
```env
EMAIL_USER=myemail@gmail.com
EMAIL_PASS=abcd efgh ijkl mnop  # Пароль приложения из Gmail
```

---

## 🚀 Для продакшена (на сервере)

На сервере создай `.env` файл в директории проекта:

```bash
# На сервере
cd /var/www/centraldispatch.id
nano .env
```

**Содержимое для продакшена:**
```env
NODE_ENV=production
PORT=3000
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password
EMAIL_TO=gabrielyankaro67@gmail.com
NEXT_PUBLIC_API_URL=http://centraldispatch.id
```

---

## ✅ Проверка

После создания `.env` файла:

1. Перезапусти dev сервер (если запущен):
   ```bash
   npm run dev
   ```

2. Проверь что переменные загружаются:
   ```bash
   # В Node.js можно проверить:
   console.log(process.env.EMAIL_HOST)
   ```

---

## 📌 Важно

- ❌ **НЕ коммить** `.env` файл в Git
- ✅ Используй `.env.example` как шаблон (без реальных паролей)
- ✅ Храни реальные пароли только в `.env` (который в `.gitignore`)
- ✅ На сервере создай `.env` отдельно

---

**После создания `.env` файла проект должен работать с правильными настройками!** 🎉


