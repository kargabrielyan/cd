# 🌐 Настройка DNS в Hostinger для подключения домена к серверу

## 📋 Что нужно сделать в Hostinger

### Шаг 1: Войти в панель управления Hostinger

1. Зайдите на https://hpanel.hostinger.com/
2. Войдите в свой аккаунт
3. Найдите ваш домен `centralldispatch.id` в списке доменов

### Шаг 2: Настроить DNS записи

1. **Найдите раздел "DNS" или "DNS Management"** в панели управления доменом
2. **Добавьте или измените A-запись:**

   **Для основного домена:**
   - **Тип:** `A`
   - **Имя:** `@` (или оставьте пустым, или `centralldispatch.id`)
   - **Значение (Value/Points to):** `89.117.151.7`
   - **TTL:** `3600` (или Auto/Default)

   **Для www поддомена:**
   - **Тип:** `A`
   - **Имя:** `www`
   - **Значение (Value/Points to):** `89.117.151.7`
   - **TTL:** `3600` (или Auto/Default)

### Шаг 3: Сохранить изменения

1. Нажмите "Сохранить" или "Add Record"
2. Подождите 5-60 минут для распространения DNS (обычно 10-15 минут)

### Шаг 4: Проверить DNS

После настройки DNS, проверьте что домен указывает на ваш сервер:

```bash
# На вашем компьютере или на сервере
nslookup centralldispatch.id
# или
dig centralldispatch.id +short
```

**Ожидаемый результат:** Должен показать `89.117.151.7`

---

## 🔍 Альтернативный вариант: Использовать Nameservers

Если в Hostinger есть возможность изменить Nameservers, можно использовать:

1. **Найти раздел "Nameservers" или "DNS Nameservers"**
2. **Изменить на:**
   - `ns1.digitalocean.com`
   - `ns2.digitalocean.com`
   - `ns3.digitalocean.com`

Но проще использовать DNS записи напрямую в Hostinger.

---

## ⚠️ Важно!

- **Не используйте Cloudflare Proxy** на этом этапе (если есть такая опция в Hostinger)
- **TTL** можно оставить по умолчанию или установить 3600 секунд
- **Подождите 10-60 минут** после изменения DNS перед проверкой

---

## ✅ После настройки DNS

Когда DNS настроен и домен указывает на `89.117.151.7`, выполните на сервере:

```bash
# 1. Создать конфигурацию Nginx
# 2. Установить SSL сертификат через Certbot
# 3. Настроить webhook для Telegram бота
```

---

## 📝 Скриншоты (примерные названия в Hostinger)

В Hostinger DNS настройки могут называться:
- **DNS Zone Editor**
- **DNS Management**
- **Advanced DNS**
- **DNS Records**

Ищите раздел где можно добавить записи типа `A`, `AAAA`, `CNAME`, `MX` и т.д.










