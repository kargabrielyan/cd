# 🚀 Деплой Next.js приложения на сервере

**Проблема:** На сервере запущен простой Express сервер, который возвращает только JSON, а нужно запустить Next.js приложение.

**Решение:** Собрать и запустить Next.js приложение через PM2.

---

## 📋 Предварительные требования

- ✅ Node.js установлен (через NVM)
- ✅ PM2 установлен
- ✅ Nginx настроен и проксирует на порт 3000
- ✅ Проект загружен на GitHub

---

## 🔧 Шаг 1: Подключиться к серверу

```bash
ssh centraldispatch
# или
ssh root@72.60.31.149
```

---

## 🔧 Шаг 2: Перейти в директорию проекта

```bash
# Если проект в /var/www/centraldispatch.id
cd /var/www/centraldispatch.id

# Или если в другой директории, найди где проект:
find /var/www -name "package.json" -type f 2>/dev/null
find /root -name "package.json" -type f 2>/dev/null
```

---

## 🔧 Шаг 3: Получить последнюю версию проекта

```bash
# Если проект в Git репозитории
git pull origin main

# Или клонировать заново (если нужно)
# git clone https://github.com/SipanBabajanyan/server.git .
```

---

## 🔧 Шаг 4: Загрузить NVM (если нужно)

```bash
# Загрузить NVM
source ~/.nvm/nvm.sh

# Проверить версию Node.js
node -v
npm -v

# Если Node.js не установлен, установить:
# nvm install 18
# nvm use 18
```

---

## 🔧 Шаг 5: Установить зависимости

```bash
# Установить все зависимости (включая devDependencies для сборки)
npm install

# Проверить что зависимости установлены
ls -la node_modules/ | head -5
```

---

## 🔧 Шаг 6: Создать .env файл (если нужно)

```bash
# Проверить есть ли .env файл
ls -la .env

# Если нет, создать:
nano .env
```

**Минимальный .env для Next.js:**
```
NODE_ENV=production
PORT=3000
NEXT_PUBLIC_API_URL=http://centraldispatch.id
```

**Сохранить:** `Ctrl + O` → `Enter` → `Ctrl + X`

---

## 🔧 Шаг 7: Собрать Next.js приложение

```bash
# Собрать приложение для продакшена
npm run build

# Проверить что сборка успешна
ls -la .next/

# Должна быть папка .next/ с собранными файлами
```

**⚠️ Если есть ошибки сборки:**
```bash
# Посмотреть детальные ошибки
npm run build 2>&1 | tee build.log

# Проверить логи
cat build.log
```

---

## 🔧 Шаг 8: Остановить старый сервер (если запущен)

```bash
# Проверить что запущено
pm2 status

# Остановить старый Express сервер (если есть)
pm2 stop centraldispatch-server
pm2 delete centraldispatch-server

# Или остановить все процессы
# pm2 stop all
```

---

## 🔧 Шаг 9: Запустить Next.js приложение через PM2

```bash
# Запустить Next.js в продакшен режиме
pm2 start npm --name "centraldispatch-nextjs" -- start

# Или с явным указанием порта:
# PORT=3000 pm2 start npm --name "centraldispatch-nextjs" -- start

# Сохранить конфигурацию PM2
pm2 save

# Проверить статус
pm2 status

# Посмотреть логи
pm2 logs centraldispatch-nextjs --lines 50
```

---

## 🔧 Шаг 10: Проверить работу

```bash
# 1. Проверить локально на сервере
curl http://localhost:3000

# Должен вернуть HTML (не JSON!)

# 2. Проверить через домен
curl http://centraldispatch.id

# 3. Проверить в браузере
# Открой: http://centraldispatch.id
```

**✅ Ожидаемый результат:**
- HTML страница приложения (не JSON)
- Страница входа `/sign-in` (судя по коду, главная страница редиректит на `/sign-in`)

---

## 🔧 Шаг 11: Настроить автозапуск PM2

```bash
# Сохранить текущую конфигурацию
pm2 save

# Настроить автозапуск при перезагрузке сервера
pm2 startup

# Выполнить команду, которую покажет pm2 startup
# (обычно что-то вроде: sudo env PATH=... pm2 startup systemd -u root --hp /root)
```

---

## 🔄 Обновление приложения в будущем

Когда нужно обновить код:

```bash
# 1. Перейти в директорию проекта
cd /var/www/centraldispatch.id

# 2. Получить последние изменения
git pull origin main

# 3. Установить новые зависимости (если изменились)
npm install

# 4. Пересобрать приложение
npm run build

# 5. Перезапустить через PM2
pm2 restart centraldispatch-nextjs

# Или если нужно полностью перезапустить:
pm2 delete centraldispatch-nextjs
pm2 start npm --name "centraldispatch-nextjs" -- start
pm2 save
```

---

## 🐛 Решение проблем

### Проблема 1: Ошибки при сборке

```bash
# Проверить версию Node.js (нужна >= 18)
node -v

# Очистить кеш и пересобрать
rm -rf .next node_modules
npm install
npm run build
```

### Проблема 2: Приложение не запускается

```bash
# Проверить логи
pm2 logs centraldispatch-nextjs --lines 100

# Проверить что порт 3000 свободен
netstat -tlnp | grep 3000

# Проверить .env файл
cat .env
```

### Проблема 3: Все еще показывается JSON вместо HTML

```bash
# Проверить что запущен правильный процесс
pm2 list

# Проверить что это Next.js, а не Express
pm2 describe centraldispatch-nextjs

# Проверить логи
pm2 logs centraldispatch-nextjs

# Убедиться что старый Express сервер остановлен
pm2 stop all
pm2 delete all
pm2 start npm --name "centraldispatch-nextjs" -- start
```

### Проблема 4: Ошибки памяти при сборке

```bash
# Увеличить лимит памяти Node.js
export NODE_OPTIONS="--max-old-space-size=4096"
npm run build

# Или через PM2 с увеличенной памятью:
pm2 start npm --name "centraldispatch-nextjs" --node-args="--max-old-space-size=4096" -- start
```

---

## 📊 Полезные команды PM2

```bash
# Статус приложения
pm2 status

# Логи в реальном времени
pm2 logs centraldispatch-nextjs

# Мониторинг
pm2 monit

# Перезапустить
pm2 restart centraldispatch-nextjs

# Остановить
pm2 stop centraldispatch-nextjs

# Удалить
pm2 delete centraldispatch-nextjs

# Сохранить конфигурацию
pm2 save
```

---

## ✅ Чеклист деплоя

- [ ] Подключен к серверу
- [ ] Перешел в директорию проекта
- [ ] Получил последнюю версию из Git
- [ ] Загружен NVM и Node.js
- [ ] Установлены зависимости (`npm install`)
- [ ] Создан `.env` файл
- [ ] Приложение собрано (`npm run build`)
- [ ] Старый сервер остановлен
- [ ] Next.js запущен через PM2
- [ ] PM2 конфигурация сохранена
- [ ] Приложение отвечает на `http://localhost:3000` (HTML, не JSON)
- [ ] Сайт работает через домен `http://centraldispatch.id`

---

**После выполнения всех шагов твой Next.js проект должен работать на сервере!** 🎉

