# 🌐 Глобальная настройка переменных окружения на сервере

**Цель:** Настроить переменные окружения глобально на сервере через PM2 ecosystem файл, чтобы все настройки были централизованы и не зависели от `.env` файла.

---

## 📋 Варианты настройки

### Вариант 1: PM2 Ecosystem файл (Рекомендуется) ⭐

**Преимущества:**
- ✅ Централизованное управление всеми переменными
- ✅ Легко обновлять через Git
- ✅ Работает для всех процессов PM2
- ✅ Можно версионировать в Git

**Недостатки:**
- ⚠️ Нужно перезапускать PM2 после изменений

### Вариант 2: Systemd Environment файл

**Преимущества:**
- ✅ Глобальные настройки для всей системы
- ✅ Работает для всех сервисов

**Недостатки:**
- ⚠️ Сложнее управлять
- ⚠️ Нужны права root

### Вариант 3: .env файл в проекте

**Преимущества:**
- ✅ Простота использования
- ✅ Работает локально и на сервере

**Недостатки:**
- ⚠️ Не коммитится в Git (в .gitignore)
- ⚠️ Нужно создавать отдельно на каждом сервере

---

## 🚀 Рекомендуемое решение: PM2 Ecosystem файл

### Шаг 1: Подключиться к серверу

```bash
ssh centraldispatch
# или
ssh root@72.60.31.149
```

### Шаг 2: Перейти в директорию проекта

```bash
cd /var/www/centraldispatch.id
```

### Шаг 3: Получить последнюю версию из Git

```bash
git pull origin main
```

### Шаг 4: Создать или обновить ecosystem.config.js

```bash
# Если файла нет, скопировать из шаблона
if [ ! -f ecosystem.config.js ]; then
  cp ecosystem.config.example.js ecosystem.config.js
  echo "✅ Файл ecosystem.config.js создан из шаблона"
fi

# Проверить что файл существует
ls -la ecosystem.config.js

# Посмотреть содержимое
cat ecosystem.config.js
```

### Шаг 5: Обновить настройки в ecosystem.config.js

```bash
nano ecosystem.config.js
```

**Важно:** Обнови следующие переменные:
- `EMAIL_USER` - твой Gmail адрес
- `EMAIL_PASS` - пароль приложения Gmail
- `EMAIL_TO` - адрес получателя (gabrielyankaro67@gmail.com)

**Сохранить:** `Ctrl + O` → `Enter` → `Ctrl + X`

### Шаг 6: Остановить текущий процесс PM2

```bash
# Проверить текущие процессы
pm2 status

# Остановить и удалить старый процесс
pm2 stop centraldispatch-nextjs
pm2 delete centraldispatch-nextjs
```

### Шаг 7: Запустить через ecosystem файл

```bash
# Запустить с глобальными настройками из ecosystem.config.js
pm2 start ecosystem.config.js

# Сохранить конфигурацию PM2
pm2 save

# Проверить статус
pm2 status

# Посмотреть логи
pm2 logs centraldispatch-nextjs --lines 50
```

### Шаг 8: Проверить что переменные окружения загружены

```bash
# Посмотреть переменные окружения процесса
pm2 describe centraldispatch-nextjs | grep -A 20 "env:"

# Или проверить в логах
pm2 logs centraldispatch-nextjs | grep EMAIL
```

---

## 🔄 Обновление настроек в будущем

Когда нужно обновить настройки:

```bash
# 1. Перейти в директорию проекта
cd /var/www/centraldispatch.id

# 2. Получить последние изменения (если обновлял в Git)
git pull origin main

# 3. Отредактировать ecosystem.config.js (если нужно)
nano ecosystem.config.js

# 4. Перезапустить PM2 с новыми настройками
pm2 restart ecosystem.config.js

# Или полностью перезапустить:
pm2 delete centraldispatch-nextjs
pm2 start ecosystem.config.js
pm2 save
```

---

## 📝 Структура ecosystem.config.js

```javascript
module.exports = {
  apps: [
    {
      name: "centraldispatch-nextjs",
      script: "npm",
      args: "start",
      cwd: "/var/www/centraldispatch.id",
      env: {
        // Все переменные окружения здесь
        EMAIL_TO: "gabrielyankaro67@gmail.com",
        EMAIL_USER: "ustrucking966@gmail.com",
        // ... и т.д.
      },
    },
  ],
};
```

---

## ✅ Преимущества глобальной настройки

1. **Централизация:** Все настройки в одном месте
2. **Версионирование:** Можно коммитить в Git (без паролей)
3. **Простота:** Легко обновлять и поддерживать
4. **Надежность:** PM2 автоматически применяет настройки при перезапуске

---

## 🐛 Решение проблем

### Проблема 1: Переменные не применяются

```bash
# Проверить что процесс запущен через ecosystem файл
pm2 describe centraldispatch-nextjs

# Полностью перезапустить
pm2 delete centraldispatch-nextjs
pm2 start ecosystem.config.js
pm2 save
```

### Проблема 2: Ошибки в ecosystem.config.js

```bash
# Проверить синтаксис файла
node -c ecosystem.config.js

# Если есть ошибки, исправить
nano ecosystem.config.js
```

### Проблема 3: Конфликт с .env файлом

Если есть `.env` файл, он может переопределить настройки из ecosystem файла. 

**Решение:** Удалить или переименовать `.env` файл, если используешь ecosystem:

```bash
# Переименовать .env (на всякий случай)
mv .env .env.backup

# Или удалить (если уверен)
rm .env
```

---

## 📌 Важно

- ⚠️ **Не коммить пароли в Git!** Используй переменные окружения или секреты
- ✅ **Обновляй ecosystem.config.js** через Git, но пароли меняй вручную на сервере
- ✅ **Используй PM2 save** после каждого изменения
- ✅ **Проверяй логи** после перезапуска

---

**После настройки все переменные окружения будут глобальными и централизованными!** 🎉

