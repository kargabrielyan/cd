# 🔓 Открытие порта 3000 для прямого доступа

**⚠️ ВНИМАНИЕ:** Открытие порта приложения напрямую - это **плохая практика безопасности**!

**Рекомендация:** Оставь порт 3000 закрытым и используй только доступ через Nginx (порты 80/443).

---

## ❓ Зачем открывать порт 3000?

Обычно это НЕ нужно, потому что:
- ✅ Nginx уже проксирует запросы на приложение
- ✅ Это безопаснее (приложение скрыто за reverse proxy)
- ✅ Можно настроить SSL только на Nginx
- ✅ Nginx может кешировать и оптимизировать запросы

**Но если тебе действительно нужно** (например, для отладки или специальных случаев):

---

## 🔧 Вариант 1: Открыть порт через firewalld

```bash
# 1. Проверить статус firewall
firewall-cmd --list-all

# 2. Открыть порт 3000
firewall-cmd --permanent --add-port=3000/tcp

# 3. Перезагрузить firewall
firewall-cmd --reload

# 4. Проверить что порт открыт
firewall-cmd --list-ports
```

---

## 🔧 Вариант 2: Открыть порт через ufw (если используется ufw)

```bash
# 1. Проверить статус ufw
ufw status

# 2. Открыть порт 3000
ufw allow 3000/tcp

# 3. Проверить правила
ufw status numbered
```

---

## 🔧 Вариант 3: Проверить настройки приложения

Убедись, что приложение слушает на всех интерфейсах (0.0.0.0), а не только на localhost:

```bash
# Проверить на каком интерфейсе слушает приложение
netstat -tlnp | grep 3000
# или
ss -tlnp | grep 3000

# Должно быть: 0.0.0.0:3000 или :::3000
# НЕ должно быть: 127.0.0.1:3000
```

Если приложение слушает только на `127.0.0.1`, нужно изменить код:

```javascript
// В app.js или server.js
app.listen(PORT, '0.0.0.0', () => {
  console.log(`Server is running on http://0.0.0.0:${PORT}`);
});
```

---

## ✅ Проверка после открытия порта

```bash
# 1. Проверить что порт открыт в firewall
firewall-cmd --list-ports  # для firewalld
# или
ufw status  # для ufw

# 2. Проверить что приложение слушает на всех интерфейсах
netstat -tlnp | grep 3000

# 3. Проверить доступность извне (с другого компьютера)
curl http://72.60.31.149:3000
```

---

## 🔒 Безопасность: Рекомендуемая конфигурация

**Правильная настройка для продакшена:**

1. ✅ Порт 3000 закрыт для внешнего доступа
2. ✅ Приложение слушает на `0.0.0.0:3000` (для доступа через localhost)
3. ✅ Nginx проксирует запросы с портов 80/443 на порт 3000
4. ✅ SSL настроен на Nginx (не на приложении)

**Схема:**
```
Интернет → Nginx (порт 80/443) → Node.js (порт 3000, только localhost)
```

---

## 🐛 Если порт все равно не открывается

```bash
# 1. Проверить что приложение запущено
pm2 status
pm2 logs centraldispatch-server --lines 20

# 2. Проверить что порт слушается
netstat -tlnp | grep 3000

# 3. Проверить firewall правила
firewall-cmd --list-all
# или
ufw status verbose

# 4. Проверить логи firewall
journalctl -u firewalld -n 50  # для firewalld
# или
tail -50 /var/log/ufw.log  # для ufw

# 5. Проверить что порт не заблокирован на уровне провайдера
# (некоторые провайдеры блокируют нестандартные порты)
```

---

## 📝 Итоговая рекомендация

**Оставь порт 3000 закрытым!** Это правильная и безопасная конфигурация.

Если нужен доступ к приложению:
- ✅ Используй домен: `http://centraldispatch.id`
- ✅ Используй IP через Nginx: `http://72.60.31.149` (если настроен default server)
- ❌ НЕ открывай порт 3000 напрямую

**Текущая конфигурация работает правильно!** 🎉

