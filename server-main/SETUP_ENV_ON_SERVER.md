# 🔧 Настройка .env файла на сервере

**Важно:** `.env` файл нужно создать на сервере отдельно, он не копируется автоматически из Git (он в `.gitignore`).

---

## 📋 Шаг 1: Подключиться к серверу

```bash
ssh centraldispatch
# или
ssh root@72.60.31.149
```

---

## 📋 Шаг 2: Перейти в директорию проекта

```bash
cd /var/www/centraldispatch.id
# или найти где проект:
find /var/www -name "package.json" -type f 2>/dev/null
```

---

## 📋 Шаг 3: Создать .env файл

```bash
# Создать .env файл
nano .env
```

---

## 📋 Шаг 4: Вставить настройки

Скопируй и вставь следующее содержимое (замени `vwhj wwmt ktkw acya` на реальный пароль приложения Gmail, если нужно):

```env
# Режим работы
NODE_ENV=production

# Порт для Next.js сервера
PORT=3000

# Настройки Email для отправки кодов подтверждения
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=ustrucking966@gmail.com
EMAIL_PASS=vwhj wwmt ktkw acya
EMAIL_TO=gabrielyankaro67@gmail.com

# URL API (для продакшена)
NEXT_PUBLIC_API_URL=http://centraldispatch.id

# Basic Auth (опционально)
ENABLE_BASIC_AUTH=false
BASIC_AUTH_USER=tester
BASIC_AUTH_PASS=test123
```

**Как сохранить в nano:**
- `Ctrl + O` → `Enter` → `Ctrl + X`

---

## 📋 Шаг 5: Проверить что файл создан

```bash
# Проверить содержимое
cat .env

# Проверить права доступа
ls -la .env
```

---

## 📋 Шаг 6: Перезапустить приложение

```bash
# Перезапустить PM2 процесс
pm2 restart centraldispatch-nextjs

# Или если процесс называется по-другому:
pm2 restart all

# Проверить логи
pm2 logs centraldispatch-nextjs --lines 20
```

---

## ✅ Проверка работы Email

После перезапуска приложения Email должен работать. Проверить можно:

1. Попробовать отправить код подтверждения через форму на сайте
2. Проверить логи приложения:
   ```bash
   pm2 logs centraldispatch-nextjs --lines 50
   ```
3. Проверить логи Email (если есть ошибки, они будут в логах)

---

## 🐛 Если Email не работает

### Проблема 1: Неправильный пароль приложения

```bash
# Проверить .env файл
cat .env | grep EMAIL_PASS

# Убедись что пароль правильный (16 символов, разделенных пробелами)
# Формат: xxxx xxxx xxxx xxxx
```

**Как получить правильный пароль:**
1. Перейди: https://myaccount.google.com/apppasswords
2. Создай пароль для "Почта"
3. Скопируй пароль (формат: `abcd efgh ijkl mnop`)
4. Обнови `.env` файл на сервере

### Проблема 2: Двухфакторная аутентификация не включена

Пароль приложения работает только если включена двухфакторная аутентификация в Google аккаунте.

### Проблема 3: Приложение не перезапустилось

```bash
# Полностью перезапустить
pm2 delete centraldispatch-nextjs
pm2 start npm --name "centraldispatch-nextjs" -- start
pm2 save
```

### Проблема 4: Ошибки в логах

```bash
# Посмотреть детальные логи
pm2 logs centraldispatch-nextjs --lines 100

# Искать ошибки связанные с Email
pm2 logs centraldispatch-nextjs | grep -i email
pm2 logs centraldispatch-nextjs | grep -i error
```

---

## 📝 Быстрое создание .env на сервере (одной командой)

Если хочешь создать файл быстро, выполни:

```bash
cd /var/www/centraldispatch.id && \
cat > .env << 'EOF'
NODE_ENV=production
PORT=3000
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=ustrucking966@gmail.com
EMAIL_PASS=vwhj wwmt ktkw acya
EMAIL_TO=gabrielyankaro67@gmail.com
NEXT_PUBLIC_API_URL=http://centraldispatch.id
ENABLE_BASIC_AUTH=false
BASIC_AUTH_USER=tester
BASIC_AUTH_PASS=test123
EOF
```

Затем перезапусти приложение:
```bash
pm2 restart centraldispatch-nextjs
```

---

## ✅ Чеклист

- [ ] Подключен к серверу
- [ ] Перешел в директорию проекта
- [ ] Создан `.env` файл
- [ ] Вставлены правильные настройки Email
- [ ] Файл сохранен
- [ ] Приложение перезапущено через PM2
- [ ] Проверены логи на ошибки
- [ ] Email работает (проверено отправкой кода)

---

**После выполнения этих шагов Email будет работать на домене!** 🎉

