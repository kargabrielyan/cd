# 🔄 Обновление проекта на сервере из Git

**Задача:** Получить последние изменения из репозитория и обновить приложение на сервере

---

## 🚀 Команды для выполнения на сервере

Подключись к серверу и выполни:

```bash
# 1. Подключиться к серверу
ssh root@72.60.31.149
# или
ssh centraldispatch

# 2. Перейти в директорию проекта
cd /var/www/centraldispatch.id

# 3. Загрузить NVM (если нужно)
source ~/.nvm/nvm.sh

# 4. Получить последние изменения из Git
git pull origin main

# 5. Установить новые зависимости (если package.json изменился)
npm install

# 6. Для Next.js: пересобрать приложение
npm run build

# 7. Перезапустить приложение через PM2
pm2 restart centraldispatch-nextjs
# или если процесс называется по-другому:
pm2 restart all

# 8. Проверить статус
pm2 status

# 9. Проверить логи (если есть ошибки)
pm2 logs centraldispatch-nextjs --lines 50
```

---

## ✅ Быстрая команда (все в одной строке)

```bash
cd /var/www/centraldispatch.id && \
source ~/.nvm/nvm.sh && \
git pull origin main && \
npm install && \
npm run build && \
pm2 restart centraldispatch-nextjs && \
pm2 save && \
echo "✅ Обновление завершено!"
```

---

## 🔍 Проверка после обновления

```bash
# 1. Проверить что приложение работает
curl http://localhost:3000

# 2. Проверить через домен
curl https://centraldispatch.id/sign-in

# 3. Проверить логи на ошибки
pm2 logs centraldispatch-nextjs --lines 20
```

---

## 🐛 Если что-то пошло не так

### Проблема: Git pull не работает

```bash
# Проверить статус Git
git status

# Проверить remote
git remote -v

# Если нужно, настроить remote заново
git remote set-url origin https://github.com/kargabrielyan/cd.git
```

### Проблема: Ошибки при сборке

```bash
# Очистить кеш и пересобрать
rm -rf .next
rm -rf node_modules
npm install
npm run build
```

### Проблема: Приложение не запускается

```bash
# Проверить логи
pm2 logs centraldispatch-nextjs --lines 100

# Перезапустить приложение
pm2 delete centraldispatch-nextjs
cd /var/www/centraldispatch.id
source ~/.nvm/nvm.sh
npm run build
pm2 start npm --name "centraldispatch-nextjs" -- start
pm2 save
```

---

## 📝 Чеклист обновления

- [ ] Подключился к серверу
- [ ] Перешел в директорию проекта (`cd /var/www/centraldispatch.id`)
- [ ] Загрузил NVM (`source ~/.nvm/nvm.sh`)
- [ ] Выполнил `git pull origin main`
- [ ] Установил зависимости (`npm install`)
- [ ] Пересобрал приложение (`npm run build`)
- [ ] Перезапустил PM2 (`pm2 restart centraldispatch-nextjs`)
- [ ] Проверил статус (`pm2 status`)
- [ ] Проверил работу сайта (`https://centraldispatch.id/sign-in`)

---

**После выполнения этих шагов изменения будут видны на сайте!** 🎉

