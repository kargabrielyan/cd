# 📦 Инструкция: Загрузка проекта на GitHub и запуск на сервере

> **📌 Для полной настройки домена с Cloudflare Proxy смотри:** [`5.DOMAIN_SETUP_CLOUDFLARE.md`](./5.DOMAIN_SETUP_CLOUDFLARE.md)

> **🚀 Для деплоя Next.js приложения смотри:** [`DEPLOY_NEXTJS.md`](./DEPLOY_NEXTJS.md) или быструю инструкцию [`QUICK_DEPLOY_NEXTJS.md`](./QUICK_DEPLOY_NEXTJS.md)

## Шаг 1: Загрузить проект на GitHub

### На локальном компьютере:

```bash
# 1. Перейти в директорию проекта
cd C:\AI\CentralDispatch

# 2. Инициализировать Git репозиторий (если еще не инициализирован)
git init

# 3. Добавить все файлы
git add .

# 4. Создать первый коммит
git commit -m "Initial commit: Test Node.js server"

# 5. Добавить remote репозиторий
git remote add origin https://github.com/SipanBabajanyan/server.git

# 6. Переименовать ветку в main (если нужно)
git branch -M main

# 7. Загрузить на GitHub
git push -u origin main
```

**Если репозиторий уже существует и есть файлы:**
```bash
# Принудительно загрузить (ОСТОРОЖНО!)
git push -u origin main --force
```

---

## Шаг 2: Клонировать проект на сервере

### На сервере (SSH подключение):

```bash
# 1. Перейти в директорию проекта
cd /var/www/centraldispatch.id

# 2. Клонировать репозиторий
git clone https://github.com/kargabrielyan/cd .

# Или если директория не пустая:
# git clone https://github.com/SipanBabajanyan/server.git temp
# cp -r temp/* .
# rm -rf temp
```

---

## Шаг 3: Установить зависимости

**⚠️ Важно:** Если это Next.js проект, используй инструкцию [`DEPLOY_NEXTJS.md`](./DEPLOY_NEXTJS.md)

```bash
# Для простого Express сервера:
npm install --production

# Для Next.js проекта:
npm install  # (включая devDependencies для сборки)
```

---

## Шаг 4: Создать .env файл

```bash
# Создать .env файл
nano .env
```

Вставить:
```
NODE_ENV=production
PORT=3000
```

Сохранить: `Ctrl + O` → `Enter` → `Ctrl + X`

---

## Шаг 5: Запустить приложение через PM2

**⚠️ Важно:** Если это Next.js проект, сначала нужно собрать приложение:
```bash
npm run build
```

```bash
# Для простого Express сервера:
pm2 start app.js --name "centraldispatch-server"

# Для Next.js проекта:
pm2 start npm --name "centraldispatch-nextjs" -- start

# Сохранить конфигурацию PM2
pm2 save

# Проверить статус
pm2 status

# Посмотреть логи
pm2 logs centraldispatch-server  # для Express
# или
pm2 logs centraldispatch-nextjs   # для Next.js
```

---

## Шаг 6: Проверить что работает

### Проверить локально на сервере:

```bash
# Проверить что приложение отвечает
curl http://localhost:3000
```

**⚠️ Важно:** Порт 3000 обычно закрыт для внешнего доступа через firewall (это правильно для безопасности!).

### Проверить через Nginx (основной способ):

```bash
# Через домен (если DNS работает)
curl http://centraldispatch.id
```

Или открой в браузере: `http://centraldispatch.id`

**✅ Ожидаемый результат:** JSON ответ от приложения:
```json
{"message":"11111🚀 Server is running successfully!","timestamp":"...","environment":"development","port":3000}
```

**📝 Примечание:** Если `http://72.60.31.149:3000` не открывается, это нормально! 
- Firewall блокирует прямой доступ к порту 3000 (правильная настройка безопасности)
- Приложение доступно только через Nginx на портах 80/443
- Если нужно открыть порт 3000 (не рекомендуется), смотри: [`OPEN_PORT_3000.md`](./OPEN_PORT_3000.md)

---

## Шаг 7: Установить SSL (когда DNS заработает)

**⚠️ Важно:** Если используешь Cloudflare Proxy, смотри полную инструкцию: [`5.DOMAIN_SETUP_CLOUDFLARE.md`](./5.DOMAIN_SETUP_CLOUDFLARE.md)

```bash
# Установить Certbot (если еще не установлен)
apt install -y certbot python3-certbot-nginx

# Получить SSL сертификат (неинтерактивный режим)
certbot --nginx -d centraldispatch.id \
  --non-interactive \
  --agree-tos \
  --email admin@centraldispatch.id \
  --redirect

# Certbot автоматически настроит HTTPS!
# После установки SSL обнови конфигурацию Nginx для Cloudflare Proxy
# (см. 5.DOMAIN_SETUP_CLOUDFLARE.md)
```

---

## 🔄 Обновление проекта в будущем

Когда нужно обновить код на сервере:

```bash
# 1. Перейти в директорию проекта
cd /var/www/centraldispatch.id

# 2. Остановить приложение
pm2 stop centraldispatch-server

# 3. Получить последние изменения
git pull origin main

# 4. Установить новые зависимости (если изменились)
npm install --production

# 5. Запустить приложение снова
pm2 restart centraldispatch-server

# Или просто перезапустить
pm2 restart centraldispatch-server
```

---

## 📊 Полезные команды PM2

```bash
# Статус приложения
pm2 status

# Логи приложения
pm2 logs centraldispatch-server

# Мониторинг в реальном времени
pm2 monit

# Перезапустить
pm2 restart centraldispatch-server

# Остановить
pm2 stop centraldispatch-server

# Удалить из PM2
pm2 delete centraldispatch-server
```

---

## ✅ Чеклист развертывания

- [ ] Проект загружен на GitHub
- [ ] Проект клонирован на сервере
- [ ] Зависимости установлены (`npm install --production`)
- [ ] Создан `.env` файл с переменными окружения
- [ ] Приложение запущено через PM2
- [ ] PM2 конфигурация сохранена (`pm2 save`)
- [ ] Приложение отвечает на `http://localhost:3000`
- [ ] Nginx настроен (если используется)
- [ ] SSL сертификат установлен (если DNS работает)

---

**Готово!** Твой проект должен работать на продакшене! 🎉

