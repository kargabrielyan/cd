# 🔧 Исправление ошибок React и загрузки файлов

**Проблемы:**
- React error #423 (гидратация)
- 400 ошибки для JS файлов
- 404 для favicon.ico

---

## ✅ Исправления в коде

### 1. Обернуть useSearchParams в Suspense

Исправлено в:
- `app/Account/Login/page.tsx`
- `app/verification/verifycode/page.tsx`

### 2. Обновить next.config.js

Добавлена конфигурация для правильной обработки статических файлов.

---

## 🚀 Команды для обновления на сервере

```bash
# 1. Подключиться к серверу
ssh root@72.60.31.149

# 2. Перейти в директорию проекта
cd /var/www/centraldispatch.id

# 3. Загрузить NVM
source ~/.nvm/nvm.sh

# 4. Получить последние изменения
git pull origin main

# 5. Установить зависимости
npm install

# 6. Очистить старую сборку
rm -rf .next

# 7. Пересобрать приложение
npm run build

# 8. Проверить что сборка успешна
ls -la .next/

# 9. Перезапустить приложение
pm2 restart centraldispatch-nextjs

# 10. Проверить статус
pm2 status

# 11. Проверить логи
pm2 logs centraldispatch-nextjs --lines 50
```

---

## 🔍 Проверка favicon

```bash
# Проверить что favicon файлы на месте
ls -la public/favicon.*
ls -la app/favicon.ico

# Если файлов нет, скопировать:
# cp favikon-removebg-preview.png public/favicon.ico
# cp favikon-removebg-preview.png public/favicon.png
# cp favikon-removebg-preview.png app/favicon.ico
```

---

## 🐛 Если ошибки остаются

### Проблема: React error #423

Это ошибка гидратации. Убедись что:
- `useSearchParams` обернут в `Suspense`
- Нет различий между серверным и клиентским рендерингом

### Проблема: 400 ошибки для JS файлов

```bash
# Проверить что Nginx правильно проксирует статические файлы
sudo cat /etc/nginx/sites-available/centraldispatch.conf | grep "_next"

# Должно быть что-то вроде:
# location /_next/static {
#   proxy_pass http://localhost:3000;
# }
```

### Проблема: Favicon 404

```bash
# Проверить что файл существует
ls -la public/favicon.ico

# Если нет, создать:
cd /var/www/centraldispatch.id
# Скопировать favicon файл
```

---

## ✅ Быстрая команда (все в одной строке)

```bash
cd /var/www/centraldispatch.id && \
source ~/.nvm/nvm.sh && \
git pull origin main && \
npm install && \
rm -rf .next && \
npm run build && \
pm2 restart centraldispatch-nextjs && \
pm2 save && \
echo "✅ Обновление завершено!"
```

---

**После выполнения этих шагов ошибки должны исчезнуть!** 🎉

