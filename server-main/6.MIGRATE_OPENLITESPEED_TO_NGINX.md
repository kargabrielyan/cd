# 🔄 Миграция с OpenLiteSpeed на Nginx для Node.js

**Дата создания:** Декабрь 2025  
**Сервер:** Ubuntu 24.04.3 LTS (72.60.31.149)  
**Домен:** centraldispatch.id  
**Цель:** Переход с OpenLiteSpeed на Nginx для Node.js приложения

---

## 📋 Содержание

1. [Подготовка: Проверка текущего состояния](#подготовка-проверка-текущего-состояния)
2. [Шаг 1: Резервное копирование конфигурации](#шаг-1-резервное-копирование)
3. [Шаг 2: Остановка OpenLiteSpeed](#шаг-2-остановка-openlitespeed)
4. [Шаг 3: Установка Nginx](#шаг-3-установка-nginx)
5. [Шаг 4: Настройка Nginx для Node.js](#шаг-4-настройка-nginx-для-nodejs)
6. [Шаг 5: Перенос SSL сертификатов](#шаг-5-перенос-ssl-сертификатов)
7. [Шаг 6: Настройка Firewall](#шаг-6-настройка-firewall)
8. [Шаг 7: Финальная проверка](#шаг-7-финальная-проверка)
9. [Откат изменений (если что-то пошло не так)](#откат-изменений)

---

## 🔍 Подготовка: Проверка текущего состояния

Перед началом миграции проверь текущее состояние сервера:

```bash
# 1. Подключиться к серверу
ssh root@72.60.31.149

# 2. Проверить статус Node.js приложения (PM2)
source ~/.nvm/nvm.sh 2>/dev/null || export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
pm2 status

# 3. Проверить на каком порту работает Node.js
netstat -tlnp | grep node || ss -tlnp | grep node

# 4. Проверить текущие SSL сертификаты (если есть)
ls -la /usr/local/lsws/conf/cert/ 2>/dev/null || echo "SSL сертификаты не найдены в OpenLiteSpeed"

# 5. Проверить конфигурацию OpenLiteSpeed
ls -la /usr/local/lsws/conf/vhosts/ 2>/dev/null || echo "Конфигурация не найдена"

# 6. Проверить что приложение работает
curl http://localhost:3000

# 7. Проверить текущий домен
curl -I http://centraldispatch.id
```

**Запиши результаты:**
- Порт Node.js приложения: `________` (обычно 3000)
- Путь к SSL сертификатам: `________`
- Статус OpenLiteSpeed: `________`

---

## 💾 Шаг 1: Резервное копирование

**ВАЖНО:** Создай резервные копии перед изменениями!

```bash
# 1. Создать директорию для бэкапов
mkdir -p /root/backup_openlitespeed_$(date +%Y%m%d)

# 2. Скопировать конфигурацию OpenLiteSpeed
cp -r /usr/local/lsws/conf/ /root/backup_openlitespeed_$(date +%Y%m%d)/lsws_conf/ 2>/dev/null || echo "Конфигурация не найдена"

# 3. Скопировать SSL сертификаты (если есть)
cp -r /usr/local/lsws/conf/cert/ /root/backup_openlitespeed_$(date +%Y%m%d)/ssl_certs/ 2>/dev/null || echo "SSL сертификаты не найдены"

# 4. Сохранить информацию о текущих процессах
pm2 list > /root/backup_openlitespeed_$(date +%Y%m%d)/pm2_list.txt 2>/dev/null
netstat -tlnp > /root/backup_openlitespeed_$(date +%Y%m%d)/netstat.txt 2>/dev/null

# 5. Проверить что бэкап создан
ls -la /root/backup_openlitespeed_$(date +%Y%m%d)/
```

**Ожидаемый результат:**
- Директория с бэкапом создана
- Файлы скопированы

---

## 🛑 Шаг 2: Остановка OpenLiteSpeed

```bash
# 1. Остановить OpenLiteSpeed
systemctl stop lsws

# 2. Отключить автозапуск OpenLiteSpeed
systemctl disable lsws

# 3. Проверить что порты 80 и 443 свободны
netstat -tlnp | grep -E ':(80|443)' || ss -tlnp | grep -E ':(80|443)'

# 4. Убедиться что Node.js приложение все еще работает
curl http://localhost:3000
```

**Ожидаемый результат:**
- OpenLiteSpeed остановлен (`inactive (dead)`)
- Порты 80 и 443 свободны
- Node.js приложение продолжает работать

**⚠️ ВАЖНО:** Не удаляй OpenLiteSpeed пока не убедишься что Nginx работает правильно!

---

## 🌐 Шаг 3: Установка Nginx

```bash
# 1. Обновить список пакетов
apt update

# 2. Установить Nginx
apt install -y nginx

# 3. Проверить версию Nginx
nginx -v

# 4. Запустить Nginx
systemctl start nginx

# 5. Включить автозапуск Nginx
systemctl enable nginx

# 6. Проверить что Nginx работает
curl http://localhost

# 7. Проверить конфигурацию Nginx
nginx -t
```

**Ожидаемый результат:**
- Nginx установлен и запущен
- `curl http://localhost` возвращает HTML страницу Nginx
- `nginx -t` показывает `test is successful`

---

## ⚙️ Шаг 4: Настройка Nginx для Node.js

### 4.1. Создать конфигурацию сайта

```bash
# 1. Создать конфигурацию для домена
nano /etc/nginx/sites-available/centraldispatch.id
```

### 4.2. Вставить конфигурацию

**Базовая конфигурация (HTTP, перед SSL):**

```nginx
# HTTP сервер (временно, до установки SSL)
server {
    listen 80;
    server_name centraldispatch.id www.centraldispatch.id;

    # Логи
    access_log /var/log/nginx/centraldispatch.id.access.log;
    error_log /var/log/nginx/centraldispatch.id.error.log;

    # Прокси на Node.js приложение
    # ⚠️ Если твое приложение на другом порту, измени 3000 на нужный порт!
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # WebSocket поддержка
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
        
        # Базовые заголовки
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Cloudflare Proxy поддержка (если используется)
        proxy_set_header CF-Connecting-IP $http_cf_connecting_ip;
        proxy_set_header CF-Ray $http_cf_ray;
        proxy_set_header CF-Visitor $http_cf_visitor;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Кеширование статических файлов
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

**Как сохранить в nano:**
- `Ctrl + O` → `Enter` → `Ctrl + X`

### 4.3. Активировать сайт

```bash
# 1. Создать символическую ссылку (активировать сайт)
ln -s /etc/nginx/sites-available/centraldispatch.id /etc/nginx/sites-enabled/

# 2. Удалить дефолтный сайт Nginx (опционально)
rm /etc/nginx/sites-enabled/default 2>/dev/null || echo "Дефолтный сайт не найден"

# 3. Проверить конфигурацию (ВАЖНО!)
nginx -t

# 4. Если тест успешен, перезагрузить Nginx
systemctl reload nginx

# 5. Проверить что Nginx работает
curl http://localhost -H 'Host: centraldispatch.id'
```

**Ожидаемый результат:**
- `nginx -t` показывает `test is successful`
- Nginx перезагружен без ошибок
- Сайт доступен по HTTP

### 4.4. Проверка работы

```bash
# 1. Проверить локально
curl http://localhost -H 'Host: centraldispatch.id'

# 2. Проверить через домен (если DNS настроен)
curl http://centraldispatch.id

# 3. Проверить логи
tail -20 /var/log/nginx/centraldispatch.id.access.log
```

**Ожидаемый результат:**
- Сайт отвечает через Nginx
- Логи показывают успешные запросы

---

## 🔒 Шаг 5: Перенос SSL сертификатов

### 5.1. Найти существующие SSL сертификаты

```bash
# 1. Проверить сертификаты в OpenLiteSpeed
ls -la /usr/local/lsws/conf/cert/ 2>/dev/null

# 2. Проверить сертификаты Let's Encrypt (если использовались)
ls -la /etc/letsencrypt/live/centraldispatch.id/ 2>/dev/null

# 3. Проверить другие возможные места
find /usr/local/lsws -name "*.pem" -o -name "*.crt" -o -name "*.key" 2>/dev/null
```

### 5.2. Вариант А: Если есть сертификаты Let's Encrypt

```bash
# 1. Установить Certbot (если не установлен)
apt install -y certbot python3-certbot-nginx

# 2. Получить новый сертификат через Certbot
certbot --nginx -d centraldispatch.id \
  --non-interactive \
  --agree-tos \
  --email admin@centraldispatch.id \
  --redirect

# Certbot автоматически:
# - Получит сертификат от Let's Encrypt
# - Обновит конфигурацию Nginx для HTTPS
# - Настроит редирект с HTTP на HTTPS
```

### 5.2. Вариант Б: Если есть существующие сертификаты из OpenLiteSpeed

```bash
# 1. Найти сертификаты
CERT_PATH="/usr/local/lsws/conf/cert/centraldispatch.id"
# Или другой путь, найденный на шаге 5.1

# 2. Скопировать сертификаты в стандартное место Let's Encrypt
mkdir -p /etc/letsencrypt/live/centraldispatch.id
cp $CERT_PATH/*.crt /etc/letsencrypt/live/centraldispatch.id/fullchain.pem 2>/dev/null
cp $CERT_PATH/*.key /etc/letsencrypt/live/centraldispatch.id/privkey.pem 2>/dev/null

# 3. Установить правильные права
chmod 600 /etc/letsencrypt/live/centraldispatch.id/privkey.pem
chmod 644 /etc/letsencrypt/live/centraldispatch.id/fullchain.pem

# 4. Обновить конфигурацию Nginx для HTTPS
nano /etc/nginx/sites-available/centraldispatch.id
```

**Обновленная конфигурация с SSL:**

```nginx
# HTTP -> HTTPS редирект
server {
    listen 80;
    server_name centraldispatch.id www.centraldispatch.id;
    return 301 https://$server_name$request_uri;
}

# HTTPS сервер
server {
    listen 443 ssl http2;
    server_name centraldispatch.id www.centraldispatch.id;

    # SSL сертификаты
    ssl_certificate /etc/letsencrypt/live/centraldispatch.id/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/centraldispatch.id/privkey.pem;
    
    # SSL настройки (если Certbot установил)
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    # Логи
    access_log /var/log/nginx/centraldispatch.id.access.log;
    error_log /var/log/nginx/centraldispatch.id.error.log;

    # Прокси на Node.js приложение
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # WebSocket поддержка
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_cache_bypass $http_upgrade;
        
        # Базовые заголовки
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Cloudflare Proxy поддержка (если используется)
        proxy_set_header CF-Connecting-IP $http_cf_connecting_ip;
        proxy_set_header CF-Ray $http_cf_ray;
        proxy_set_header CF-Visitor $http_cf_visitor;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Кеширование статических файлов
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

```bash
# 5. Проверить конфигурацию
nginx -t

# 6. Перезагрузить Nginx
systemctl reload nginx
```

### 5.3. Проверка SSL

```bash
# 1. Проверить сертификаты
certbot certificates 2>/dev/null || ls -la /etc/letsencrypt/live/centraldispatch.id/

# 2. Проверить HTTPS
curl -I https://centraldispatch.id

# 3. Проверить в браузере
# Открой: https://centraldispatch.id
```

**Ожидаемый результат:**
- SSL сертификат работает
- HTTPS доступен
- Зеленый замочек в браузере

---

## 🔥 Шаг 6: Настройка Firewall

```bash
# 1. Проверить текущие правила firewall
ufw status verbose

# 2. Убедиться что порты 80 и 443 открыты
ufw allow 'Nginx Full'

# 3. Проверить что порты открыты
ufw status | grep Nginx

# 4. Если OpenLiteSpeed использовал другие порты, можно закрыть их
# ufw deny 7080/tcp  # Порт админ-панели OpenLiteSpeed (если не нужен)
```

**Ожидаемый результат:**
- Порты 80 и 443 открыты для Nginx
- Firewall настроен правильно

---

## ✅ Шаг 7: Финальная проверка

### 7.1. Проверка компонентов

```bash
# 1. Статус Node.js приложения
source ~/.nvm/nvm.sh 2>/dev/null || export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
pm2 status

# 2. Проверка портов
netstat -tlnp | grep -E ':(80|443|3000)'

# 3. Проверка конфигурации Nginx
nginx -t

# 4. Проверка работы приложения локально
curl http://localhost:3000
```

### 7.2. Проверка через домен

```bash
# 1. HTTP (должен редиректить на HTTPS если SSL настроен)
curl -I http://centraldispatch.id

# 2. HTTPS
curl -I https://centraldispatch.id

# 3. Содержимое
curl https://centraldispatch.id
```

### 7.3. Проверка в браузере

1. Открой `https://centraldispatch.id`
2. Должен быть зеленый замочек (HTTPS работает)
3. Сайт должен работать как раньше

### 7.4. Проверка логов

```bash
# Логи доступа
tail -20 /var/log/nginx/centraldispatch.id.access.log

# Логи ошибок
tail -20 /var/log/nginx/centraldispatch.id.error.log

# Логи PM2
pm2 logs centraldispatch-server --lines 20
```

**Ожидаемый результат:**
- Все компоненты работают
- Сайт доступен по HTTPS
- Логи не показывают ошибок

---

## 🔄 Откат изменений (если что-то пошло не так)

Если нужно вернуться к OpenLiteSpeed:

```bash
# 1. Остановить Nginx
systemctl stop nginx
systemctl disable nginx

# 2. Запустить OpenLiteSpeed
systemctl start lsws
systemctl enable lsws

# 3. Проверить что сайт работает
curl http://centraldispatch.id
```

**Если нужно полностью удалить Nginx:**

```bash
# 1. Остановить Nginx
systemctl stop nginx
systemctl disable nginx

# 2. Удалить Nginx
apt remove -y nginx nginx-common

# 3. Удалить конфигурацию (опционально)
rm -rf /etc/nginx/

# 4. Очистить пакеты
apt autoremove -y
```

---

## 🗑️ Удаление OpenLiteSpeed (после успешной миграции)

**⚠️ ВНИМАНИЕ:** Удаляй OpenLiteSpeed только после того, как убедишься что Nginx работает стабильно несколько дней!

```bash
# 1. Убедиться что OpenLiteSpeed остановлен
systemctl stop lsws
systemctl disable lsws

# 2. Удалить OpenLiteSpeed
# (команда зависит от способа установки)
# Если установлен через пакет:
apt remove -y openlitespeed 2>/dev/null

# Если установлен вручную:
rm -rf /usr/local/lsws/

# 3. Удалить из автозапуска (если еще не удалено)
systemctl disable lsws 2>/dev/null

# 4. Очистить пакеты
apt autoremove -y
```

---

## 📊 Полезные команды для работы с Nginx

```bash
# Перезагрузить конфигурацию
systemctl reload nginx

# Перезапустить Nginx
systemctl restart nginx

# Проверить конфигурацию
nginx -t

# Посмотреть активные сайты
ls -la /etc/nginx/sites-enabled/

# Логи в реальном времени
tail -f /var/log/nginx/centraldispatch.id.access.log
tail -f /var/log/nginx/centraldispatch.id.error.log
```

---

## 🚨 Частые проблемы и решения

### Проблема 1: Nginx показывает 502 Bad Gateway

**Причины:**
- Node.js приложение не запущено
- Неправильный порт в конфигурации

**Решение:**
```bash
# 1. Проверить что приложение запущено
pm2 status

# 2. Проверить порт
netstat -tlnp | grep :3000

# 3. Проверить конфигурацию Nginx
cat /etc/nginx/sites-available/centraldispatch.id | grep proxy_pass

# 4. Перезапустить приложение
pm2 restart centraldispatch-server
```

### Проблема 2: SSL сертификат не работает

**Решение:**
```bash
# 1. Проверить сертификат
certbot certificates

# 2. Обновить сертификат
certbot renew --force-renewal

# 3. Перезагрузить Nginx
systemctl reload nginx
```

### Проблема 3: Порты заняты

**Решение:**
```bash
# 1. Найти процесс на порту 80
lsof -i :80 || netstat -tlnp | grep :80

# 2. Остановить процесс (если это OpenLiteSpeed)
systemctl stop lsws

# 3. Проверить снова
netstat -tlnp | grep :80
```

---

## ✅ Чеклист миграции

Проверь что все выполнено:

- [ ] Резервная копия создана
- [ ] OpenLiteSpeed остановлен
- [ ] Nginx установлен и работает
- [ ] Конфигурация Nginx создана
- [ ] Сайт доступен по HTTP
- [ ] SSL сертификат установлен/перенесен
- [ ] HTTPS работает
- [ ] Node.js приложение работает через Nginx
- [ ] Firewall настроен
- [ ] Логи проверены
- [ ] Сайт протестирован в браузере
- [ ] Все работает стабильно

---

## 📝 Итоговая информация

**После миграции:**
- **Веб-сервер:** Nginx
- **Node.js менеджер:** PM2
- **Домен:** https://centraldispatch.id
- **Сервер:** 72.60.31.149
- **Конфигурация Nginx:** `/etc/nginx/sites-available/centraldispatch.id`
- **Логи Nginx:** `/var/log/nginx/centraldispatch.id.*.log`
- **SSL сертификаты:** `/etc/letsencrypt/live/centraldispatch.id/`

---

**Документ создан:** Декабрь 2025  
**Версия:** 1.0  
**Статус:** ✅ Готов к использованию

**Важно:** Выполняй шаги по порядку, не пропускай проверки! Если что-то не работает - вернись к предыдущему шагу и проверь.

