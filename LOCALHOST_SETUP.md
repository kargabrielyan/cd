# 🏠 Настройка Telegram бота для localhost

## Проблема

Telegram не может отправлять webhook на `localhost` или `127.0.0.1`. Нужен публичный URL.

## Решение: Использовать ngrok

### 1. Установка ngrok

**Windows:**
1. Скачайте ngrok с https://ngrok.com/download
2. Распакуйте в папку (например, `C:\ngrok`)
3. Добавьте в PATH или используйте полный путь

**Или через Chocolatey:**
```powershell
choco install ngrok
```

**Mac/Linux:**
```bash
brew install ngrok
# или
sudo snap install ngrok
```

### 2. Запуск ngrok туннеля

```bash
ngrok http 3000
```

Вы увидите что-то вроде:
```
Forwarding  https://abc123.ngrok.io -> http://localhost:3000
```

**Скопируйте HTTPS URL** (например: `https://abc123.ngrok.io`)

### 3. Настройка переменных окружения для localhost

Создайте файл `.env.local` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=8569212803:AAGDp_ETyyHqs_V_h2WChU184I_mGOkrJDs
TELEGRAM_CHAT_ID=5257327001
```

### 4. Настройка webhook на ngrok URL

```bash
curl -X POST "https://api.telegram.org/bot8569212803:AAGDp_ETyyHqs_V_h2WChU184I_mGOkrJDs/setWebhook?url=https://ВАШ_NGROK_URL.ngrok.io/api/telegram/webhook"
```

Замените `ВАШ_NGROK_URL` на ваш ngrok URL.

### 5. Запуск приложения

```bash
npm run dev
```

### 6. Проверка работы

1. Откройте `http://localhost:3000/sign-in`
2. Введите username и password
3. Нажмите SIGN IN
4. Должен появиться loading
5. Проверьте Telegram - должно прийти сообщение

## Важно!

⚠️ **ngrok URL меняется** при каждом перезапуске (на бесплатном плане)
⚠️ После каждого перезапуска ngrok нужно **обновить webhook** с новым URL
⚠️ Для постоянного URL используйте ngrok с авторизацией или платный план

## Альтернатива: Использовать polling вместо webhook

Можно временно использовать polling для локальной разработки, но это менее эффективно.

## Проверка webhook

```bash
curl "https://api.telegram.org/bot8569212803:AAGDp_ETyyHqs_V_h2WChU184I_mGOkrJDs/getWebhookInfo"
```

Должен вернуть ваш ngrok URL.

