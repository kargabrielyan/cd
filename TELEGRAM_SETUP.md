# 📱 Настройка Telegram бота для входа в систему

## Описание

Система входа теперь использует Telegram бота вместо Email для подтверждения входа. При попытке входа:
1. Клиент вводит username и password
2. Система отправляет уведомление в Telegram с кнопками YES/NO
3. Пока клиент ждет ответа, показывается круговой loading
4. Если нажато YES - клиент переходит на страницу verify code
5. Если нажато NO - показывается картинка "error sign in.png" и форма сбрасывается

## Шаги настройки

### 1. Создание Telegram бота

1. Откройте Telegram и найдите бота [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и создайте бота
4. Сохраните **Bot Token** (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. Получение Chat ID

1. Найдите бота [@userinfobot](https://t.me/userinfobot) в Telegram
2. Начните диалог с ботом
3. Бот отправит ваш Chat ID (например: `123456789`)

Или используйте другой способ:
1. Создайте группу и добавьте вашего бота
2. Отправьте сообщение в группу
3. Откройте в браузере: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates`
4. Найдите `"chat":{"id":123456789}` - это ваш Chat ID

### 3. Настройка Webhook

После получения Bot Token и Chat ID, настройте webhook:

```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://yourdomain.com/api/telegram/webhook"
```

Замените:
- `<YOUR_BOT_TOKEN>` на ваш Bot Token
- `https://yourdomain.com` на ваш домен

### 4. Настройка переменных окружения

Добавьте в `.env` или в переменные окружения сервера:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

### 5. Для локальной разработки (опционально)

Если нужно тестировать локально, используйте [ngrok](https://ngrok.com/) для создания туннеля:

```bash
ngrok http 3000
```

Затем настройте webhook на ngrok URL:
```bash
curl -X POST "https://api.telegram.org/bot<YOUR_BOT_TOKEN>/setWebhook?url=https://your-ngrok-url.ngrok.io/api/telegram/webhook"
```

## Проверка работы

1. Откройте форму входа на сайте
2. Введите username и password
3. Нажмите SIGN IN
4. Должен появиться круговой loading с текстом "Ожидание ответа..."
5. Проверьте Telegram - должно прийти сообщение с кнопками YES/NO
6. Нажмите YES или NO и проверьте результат

## Структура файлов

- `lib/telegram.ts` - модуль для работы с Telegram Bot API
- `lib/login-requests.ts` - хранилище pending запросов
- `app/api/telegram/webhook/route.ts` - endpoint для получения обновлений от бота
- `app/api/auth/check-status/route.ts` - endpoint для проверки статуса (long polling)
- `app/api/auth/login/route.ts` - изменен для отправки в Telegram
- `components/SignInForm/SignInForm.tsx` - изменен для polling и отображения состояний

## Важные замечания

1. **In-memory хранилище**: Текущая реализация использует in-memory хранилище для pending запросов. В production рекомендуется использовать Redis или базу данных.

2. **Таймаут**: Запросы истекают через 5 минут. Если ответ не получен, показывается ошибка.

3. **Безопасность**: Bot Token должен храниться в секрете. Никогда не коммитьте его в Git.

4. **Fallback на Email**: Если отправка в Telegram не удалась, система автоматически отправляет Email (fallback).

## Устранение неполадок

### Бот не отвечает
- Проверьте, что Bot Token правильный
- Проверьте, что webhook настроен правильно
- Проверьте логи сервера: `pm2 logs centraldispatch-nextjs`

### Сообщения не приходят
- Проверьте Chat ID
- Убедитесь, что бот добавлен в чат/группу (если используется группа)
- Проверьте переменные окружения на сервере

### Loading не исчезает
- Проверьте, что webhook endpoint доступен
- Проверьте логи в консоли браузера
- Проверьте, что polling работает (должны быть запросы к `/api/auth/check-status`)

